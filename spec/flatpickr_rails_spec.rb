require 'spec_helper'

describe FlatpickrRails do
  it 'has a version number' do
    expect(FlatpickrRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
